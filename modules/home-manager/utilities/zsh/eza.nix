{
  programs.eza = {
    enable = true;
    enableZshIntegration = true;
    icons = "auto"; # Enable icons for file types
    # eza is a modern replacement for ls.
    # It provides color support, icons, and git integration.
  };
}
