{
  programs.fzf = {
    enable = true;
    enableZshIntegration = true;
    # fzf is a general-purpose command-line fuzzy finder.
    # It's used for fuzzy searching through files, command history, etc.
  };
}
